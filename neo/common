# Settings file. This is the only file you should modify

# Directory where the new files are saved
TARBALLS_DIR="${HOME}/openSUSE/release"

# Directory where the commands list will be created
UPDATE_SCRIPT_DIR="${TARBALLS_DIR}"

# Where the all OBS repositories are checked out
OBS_DIR="${HOME}/openSUSE"

# Your OBS username
OBS_USERNAME="johndoe"

# Local git checkouts
GIT_DIR="/data/kde/src"

## repositories checkout ABSOLUTE path
# KDE:Applications
KA_CHECKOUT_DIR="${OBS_DIR}/KDE:Applications"

# KDE:Frameworks
KF_CHECKOUT_DIR="${OBS_DIR}/KDE:Frameworks5"

# KDE:Unstable:Frameworks (needed for frameworks and plasma releases)
KUF_CHECKOUT_DIR="${OBS_DIR}/KDE:Unstable:Frameworks"

##########################################################
# Modifying the parameters below should not be necessary #
##########################################################

# If upstream makes different type of releases, update the
# list_packaged_tarballs() function
RELEASE_NAME=(applications frameworks plasma)

# The tarballs extension
EXT="tar.xz"

# Where releases announces are published
ANNOUNCE_URL="https://www.kde.org/announcements"
